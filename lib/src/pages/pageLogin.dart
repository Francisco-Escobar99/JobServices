import 'package:flutter/material.dart';
import 'package:from_end/src/widgets/viewLogin.dart';

class loginPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: view_login(), //canvas
    );
  }
}
