import 'package:flutter/material.dart';
import 'package:from_end/src/widgets/viewRegister.dart';

class registerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Viewregister(), //canvas
    );
  }
}
